# Caddy Money API

Fastify + TypeScript mock API implementing restricted spending logic for the Caddy Money beta.

## Scripts

- `pnpm dev` – start dev server
- `pnpm test` – run Vitest suite
- `pnpm build` – compile TypeScript
