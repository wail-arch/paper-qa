# Caddy Money Web

Prototype mobile-first PWA for the Caddy Money restricted spending beta.

## Scripts

- `pnpm dev` – run Vite dev server.
- `pnpm build` – production build.
- `pnpm preview` – preview build.
